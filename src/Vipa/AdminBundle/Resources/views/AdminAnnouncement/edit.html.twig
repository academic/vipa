{% extends '::vipabase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.announcement_edit'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('vipa_admin_dashboard'), 'title': 'dashboard'|trans},
        {'link': path('vipa_admin_announcement_index'),'title': 'title.announcements'|trans},
        {'title': 'title.announcement_edit'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ 'title.announcement_edit'|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ form(edit_form) }}
    
    <hr>

    {{
        actions({
            'back': {'href': path('vipa_admin_announcement_index')},
        })
    }}

    {{ getLogs(entity) }}
{% endblock %}
