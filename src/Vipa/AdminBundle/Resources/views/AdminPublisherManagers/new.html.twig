{% extends '::vipabase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.publisher_managers_new'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('vipa_admin_dashboard'), 'title': 'dashboard'|trans},
    {'link': path('vipa_admin_publisher_managers_index'),'title': 'title.publisher_managerss'|trans},
    {'title': 'title.publisher_managers_new'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "publishermanagers.new"|trans }}</h1>

    <a href="{{ path('vipa_admin_publisher_managers_index') }}">{{ "b"|trans }}</a>
    {{ form(form) }}
{% endblock %}
