{% extends '::vipabase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.person_title_edit'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('vipa_admin_dashboard'), 'title': 'dashboard'|trans},
        {'link': path('vipa_admin_person_title_index'),'title': 'title.person_titles'|trans},
        {'title': 'title.person_title_edit'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ 'title.person_title_edit'|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ form(edit_form) }}

    <hr>

    {{
        actions({
            'back': {'href': path('vipa_admin_person_title_index')}
        })
    }}

    {{ getLogs(entity) }}
{% endblock %}
