{% extends '::vipabase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.article_type_new'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('dashboard'), 'title': 'dashboard'|trans},
    { 'link': path('vipa_admin_article_type_index'),'title': 'article.types'|trans},
    {'title': 'new'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "article.type.new"|trans }}</h1>

    {{ form(form) }}
    {% set back_path = 'vipa_admin_article_type_index' %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
