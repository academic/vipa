{% extends '::vipabase.html.twig' %}
{% block title %}{{ 'title.submission_file_new'|trans }} {{ parent() }}{% endblock %}


{% block breadcrumb %}
    {% set list = [
    {'link': path('vipa_journal_dashboard_index', {journalId: selectedJournal().id}), 'title': 'dashboard'|trans},
    {'link': path('vipa_journal_file_index', {'journalId': selectedJournal().id}),'title': 'submission_file.show'|trans},
    {'title': 'title.submission_file_new'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ 'title.submission_file_new'|trans }}</h1>

    {{ form(form) }}

    {{ actions({
        'back': {'href': path('vipa_journal_file_index', {'journalId': selectedJournal().id})},
    }) }}
{% endblock %}