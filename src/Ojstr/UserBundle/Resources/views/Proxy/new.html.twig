{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>proxy creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_proxy') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
