{% extends '::ojsbase.html.twig' %}

{% block body -%}
<h1>proxy clients list</h1>

<table class="table records_list">
    <thead>
        <tr>
            <th>Target User Username</th>
            <th>Target User Email</th>
            <th>Last login</th>
        </tr>
    </thead>
    <tbody>
        {% for entity in entities %}
        <tr>
            <td>{{ entity.clientUser.username }}</td>
            <td>{{ entity.clientUser.email }}</td>
            <td>{{ entity.clientUser.lastlogin|date('Y-m-d H:i:s') }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

    {% endblock %}
