{% extends '::ojsbase.html.twig' %}

{% block body -%}
<div class="large-12 columns">
    <div class="panel">
        <h1>UserJournalRole edit</h1>

    {{ form(edit_form) }}

        <a href="{{ path('ujr') }}">
            Back to the list
        </a>
         {{ form(delete_form) }} 
    </div>
</div>
{% endblock %}
