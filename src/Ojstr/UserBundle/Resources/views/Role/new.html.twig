{% extends '::ojsbase.html.twig' %}

{% block body -%}
<div class="large-12 columns">
    <div class="panel">
        <h1>{% trans %}New Role{% endtrans %}</h1>

    {{ form(form) }}

        <div class="record_actions">

            <a href="{{ path('admin_role') }}" class="button">
                Back to the list
            </a>

        </div>
    </div>
</div>
{% endblock %}
