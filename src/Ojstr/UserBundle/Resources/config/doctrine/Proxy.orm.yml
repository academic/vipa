Ojstr\UserBundle\Entity\Proxy:
    indexes:
        users_x:
          columns: proxy_user_id,target_user_id 
    type: entity
    table: null
    manyToOne:
        targetUser:
            targetEntity: User
            inversedBy: clientUsers
            joinColumn:
                name: target_user_id 
                referencedColumnName: id
        proxyUser:
            targetEntity: User
            inversedBy: proxyUsers
            joinColumn:
                name: proxy_user_id 
                referencedColumnName: id
    id:
        id:
            type: integer
            id: true
            generator:
                strategy: AUTO
    fields:
        proxyUserId:
            type: integer
            column: proxy_user_id
        targetUserId:
            type: integer
            column: target_user_id
        created:
           type: datetime
           gedmo:
             timestampable:
               on: create
        updated:
           type: datetime
           gedmo:
             timestampable:
               on: update
        deletedAt:
           type: date
           nullable: true
    gedmo:
        soft_deleteable:
            field_name: deletedAt
            time_aware: false 
        loggable: true 
    lifecycleCallbacks: {  }
