{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>ArticleProcess list</h1>

    <table class="records_list  table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Articleid</th>
                <th>Stepid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('articleprocess_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.articleId }}</td>
                <td>{{ entity.stepId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('articleprocess_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('articleprocess_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('articleprocess_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
