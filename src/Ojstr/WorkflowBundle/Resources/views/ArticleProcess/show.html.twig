{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>ArticleProcess</h1>

    <table class="record_properties  table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Articleid</th>
                <td>{{ entity.articleId }}</td>
            </tr>
            <tr>
                <th>Stepid</th>
                <td>{{ entity.stepId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('articleprocess') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('articleprocess_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
