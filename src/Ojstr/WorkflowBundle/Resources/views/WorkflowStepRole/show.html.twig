{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>WorkflowStepRole</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Stepid</th>
                <td>{{ entity.stepId }}</td>
            </tr>
            <tr>
                <th>Rolename</th>
                <td>{{ entity.roleName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('workflowsteprole') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('workflowsteprole_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
