{% extends '::ojsbase.html.twig' %}
{% block title %}{{ 'title.journal_theme_show'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('dashboard'), 'title': 'dashboard'|trans},
    {'link': path('ojs_journal_theme_index'),'title': 'journal.theme'|trans},
    {'title': 'title.journal_theme_show'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "journaltheme"|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    <table class="record_properties">
        <tbody>
        <tr>
            <th>{{ 'id'|trans }}</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>{{ "journal.id"|trans }}</th>
            <td>{{ entity.journalId }}</td>
        </tr>
        <tr>
            <th>{{ "theme.id"|trans }}</th>
            <td>{{ entity.themeId }}</td>
        </tr>
        </tbody>
    </table>

    {{
    actions({
        'back': {'href': path('ojs_journal_theme_index')},
        'edit': {'href': path('ojs_journal_theme_edit', {'id': entity.id})},
        'delete': {'href': path('ojs_journal_theme_delete', {'id': entity.id}), 'attributes': {'data-token': token}},
    })
    }}
{% endblock %}
