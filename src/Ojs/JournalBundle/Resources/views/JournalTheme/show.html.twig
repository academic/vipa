{% extends '::ojsbase.html.twig' %}
{% block title %}{{ 'title.journal_theme_show'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('dashboard'), 'title': 'dashboard'|trans},
    {'link': path('admin_journaltheme'),'title': 'journal.theme'|trans},
    {'title': 'show'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "journaltheme"|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    <table class="record_properties">
        <tbody>
        <tr>
            <th>{{ 'id'|trans }}</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>{{ "journal.id"|trans }}</th>
            <td>{{ entity.journalId }}</td>
        </tr>
        <tr>
            <th>{{ "theme.id"|trans }}</th>
            <td>{{ entity.themeId }}</td>
        </tr>
        </tbody>
    </table>
    {% set back_path = 'admin_journaltheme' %}
    {% if is_granted('EDIT', selectedJournal(), 'theme') %}
        {% set edit_path = 'admin_journaltheme_edit' %}
    {% endif %}
    {% if is_granted('DELETE', selectedJournal(), 'theme') %}
        {% set delete_path = 'admin_journaltheme_delete' %}
    {% endif %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
