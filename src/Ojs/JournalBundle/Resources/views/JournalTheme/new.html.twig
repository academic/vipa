{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"journaltheme.new"|trans}}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_journaltheme') }}">
            {{"b"|trans}}
        </a>
    </li>
</ul>
{% endblock %}
