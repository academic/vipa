{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"journaltheme.list"|trans}}</h1>

    {{ grid(grid) }}
    <hr>
    <a href="{{ path('admin_journaltheme_new') }}" class="btn btn-success">
        {{"c"|trans}}
    </a>
{% endblock %}
