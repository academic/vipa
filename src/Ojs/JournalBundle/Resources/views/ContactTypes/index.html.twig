{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"contact.type.list"|trans}}</h1>
    {% include '::flashbag.html.twig' %}
    {{ grid(grid) }}

    <a href="{{ path('contacttypes_new') }}" class="btn btn-success">{{"c"|trans}}</a>
{% endblock %}
