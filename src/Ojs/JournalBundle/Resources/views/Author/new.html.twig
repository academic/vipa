{% extends '::ojsbase.html.twig' %}
{% block breadcrumb %} 
    {{breadcrumb([ {'link': path('dashboard'), 'title': 'Dashboard'}, 
    { 'link': path('author'), 'title': 'Author Manager'},
    { 'title' : 'New Author'} ])}}
{% endblock %}

{% block body -%}
    <h1>Author creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('author') }}">
                {{"b"|trans}}
            </a>
        </li>
    </ul>
{% endblock %}
