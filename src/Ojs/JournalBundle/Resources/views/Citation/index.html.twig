{% extends '::ojsbase.html.twig' %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('dashboard'), 'title': 'dashboard'|trans},
        { 'title': 'citation.list'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{"citation.list"|trans}}</h1>
    {% include '::flashbag.html.twig' %}
    <a href="{{ path('citation_new') }}">{{"citation.new"|trans}}</a>
    {{grid(grid)}}
{% endblock %}
