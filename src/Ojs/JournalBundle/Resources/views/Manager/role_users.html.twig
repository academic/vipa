{% extends '::ojsbase.html.twig' %}
{% block title %}{{ 'title.role_users'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    { 'link': path('dashboard'), 'title': 'dashboard'|trans },
    { 'title': 'role.forusers'|trans }
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body %}

    <div class="row">
        <div class="panel">
            <h1><strong>{{ journal.title }}</strong> {{ "role.forusers"|trans }} <span class="label"></span></h1>
        </div>
    </div>

{% endblock %}
