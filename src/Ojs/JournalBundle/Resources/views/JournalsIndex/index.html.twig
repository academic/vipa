{% extends '::ojsbase.html.twig' %}

{% block body -%}
    {% include 'OjsJournalBundle:Journal:journal_settings_nav.html.twig' with {'active':2} only %}
    <h1>{{ 'journalsindex.list'|trans }}</h1>
    {{ grid(grid) }}
    <a class="btn btn-success" href="{{ path('manager_journals_indexes_new',{journal: journal_id}) }}">
        {{ 'c'|trans }}
    </a>
{% endblock %}
