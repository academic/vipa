{% extends '::ojsbase.html.twig' %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('dashboard'), 'title': 'dashboard'|trans},
        {'link': path('manager_journals_indexes'}),'title': 'journals.index'|trans},
        {'title': 'edit'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    {% include 'OjsJournalBundle:Journal:journal_settings_nav.html.twig' with {'active':2} only %}
    <h1>{{ 'journalsindex.edit'|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ form(edit_form) }}


    <a href="{{ path('manager_journals_indexes') }}">
        {{ 'b'|trans }}
    </a>
    <a href="{{ path('manager_journals_indexes') }}">
        {{ 'd'|trans }}
    </a>
{% endblock %}
