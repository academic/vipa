{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"lang.list"|trans}}</h1>
    {{ grid(grid) }}
    <a href="{{ path('lang_new') }}" class="btn btn-success">
        {{"c"|trans}}
    </a> 
{% endblock %}
