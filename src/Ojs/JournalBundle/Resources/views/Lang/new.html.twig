{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"lang.new"|trans}}</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('lang') }}">
                {{"b"|trans}}
            </a>
        </li>
    </ul>
{% endblock %}
