{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>New Language</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('lang') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
