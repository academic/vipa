{% extends '::ojsbase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.institution_theme_edit'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('ojs_user_index'), 'title': 'user_page'|trans},
        {'link': path('ojs_institution_manager_theme_index', {institutionId: institution.id}),'title': 'title.institution_theme'|trans},
        {'title': 'title.institution_theme_edit'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "institutiontheme.edit"|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ form(edit_form) }}
    {#
    {% set back_path = 'ojs_institution_manager_theme_index' %}
    {% if isGrantedForInstitution() %}
        {% set delete_path = 'ojs_institution_manager_theme_delete' %}
    {% endif %}
    {% include '::actionbuttons.html.twig' %}
    #}
    {{ getLogs(entity) }}
{% endblock %}
