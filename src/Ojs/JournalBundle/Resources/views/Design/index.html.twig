{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <h1>{{"design.list"|trans}}</h1>
    {% include '::flashbag.html.twig' %}
    {{ grid(grid) }}
    <a href="{{ path('design_new') }}" class="btn btn-success">
        {{"c"|trans}}
    </a>
{% endblock %}
