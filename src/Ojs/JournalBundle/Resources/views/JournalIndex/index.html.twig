{% extends '::ojsbase.html.twig' %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('dashboard'), 'title': 'dashboard'|trans},
        {'title': 'journal.index'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ 'journalindex.list'|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ grid(grid) }}

    <a href="{{ path('admin_journalindex_new') }}" class="btn btn-success">
        {{ 'c'|trans }}
    </a>
{% endblock %}
