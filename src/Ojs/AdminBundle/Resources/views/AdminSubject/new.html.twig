{% extends '::ojsbase.html.twig' %}
{% block title %}{{ 'title.subject_new'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('dashboard'), 'title': 'dashboard'|trans},
    {'link': path('ojs_admin_subject_index'),'title': 'subject'|trans},
    {'title': 'new'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "subject.new"|trans }}</h1>

    {{ form(form) }}

    {% set back_path = 'ojs_admin_subject_index' %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
