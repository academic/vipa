{% extends '::ojsbase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.journal_application_file'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('ojs_admin_dashboard'), 'title': 'dashboard'|trans},
        {'link': path('ojs_admin_application_file_index'),'title': 'title.journal_application_file'|trans},
        {'title': 'title.journal_application_file_show'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ entity.title|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    {{ display(entity) }}
    {% set back_path = 'ojs_admin_application_file_index' %}
    {% if is_granted('EDIT', entity) %}
        {% set edit_path = 'ojs_admin_application_file_edit' %}
    {% endif %}
    {% if is_granted('DELETE', entity) %}
        {% set delete_path = 'ojs_admin_application_file_delete' %}
    {% endif %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
