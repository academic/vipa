{% extends '::ojsbase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.default_journal_theme_new'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('ojs_admin_dashboard'), 'title': 'dashboard'|trans},
    {'link': path('ojs_admin_default_journal_theme_index'),'title': 'title.default_journal_theme'|trans},
    {'title': 'title.default_journal_theme_new'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "publishertheme.new"|trans }}</h1>

    <a href="{{ path('ojs_admin_default_journal_theme_index') }}">{{ "b"|trans }}</a>
    {{ form(form) }}
{% endblock %}
