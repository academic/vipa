{% extends '::ojsbase.html.twig' %}
{% set hideleft = true %}
{% block title %}{{ 'title.institution_theme_show'|trans }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    {% set list = [
        {'link': path('ojs_admin_dashboard'), 'title': 'dashboard'|trans},
        { 'link': path('ojs_admin_institution_theme_index'),'title': 'title.institution_theme'|trans},
        {'title': 'title.institution_theme_show'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ entity.title|trans }}</h1>
    {% include '::flashbag.html.twig' %}
    <table class="record_properties  table">
        <tbody>
        <tr>
            <th>{{ 'id'|trans }}</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>{{ "title"|trans }}</th>
            <td>{{ entity.title }}</td>
        </tr>
        <tr>
            <th>{{ "css"|trans }}</th>
            <td>{{ entity.css }}</td>
        </tr>
        </tbody>
    </table>
    {% set back_path = 'ojs_admin_institution_theme_index' %}
    {% if is_granted('EDIT', entity) %}
        {% set edit_path = 'ojs_admin_institution_theme_edit' %}
    {% endif %}
    {% if is_granted('DELETE', entity) %}
        {% set delete_path = 'ojs_admin_institution_theme_delete' %}
    {% endif %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
