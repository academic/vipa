{% extends '::ojsbase.html.twig' %}

{% block breadcrumb %}
    {% set list = [
                {'link': path('dashboard'), 'title': 'Dashboard'},
                {'title': 'Waiting Articles'}
] %}
    {{breadcrumb(list)}}
{% endblock %}

{% block body -%}
    <div class="panel">
        <div class="panel-heading">
            <h1>{{"article.articles"|trans}} / {{"article.articles"|trans}} / {{"status.waiting"|trans}} <small>These articlese submitted but waiting to be assigned.</small></h1>
        </div>

        <div class="panel-body">
            {% for entity in entities %}
                <div class="list-group-item">
                    <h4>{{entity.title}} <small>{{entity.journal.title}}</small></h4>
                    <span>
                        <!-- /* TODO : author list */  -->
                    </span>
                {% endfor %}
                <hr>
            </div>
        </div>
    </div>
{% endblock %}
