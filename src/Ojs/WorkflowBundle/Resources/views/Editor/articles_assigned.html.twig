{% extends '::ojsbase.html.twig' %}

{% block body -%}
    <div class="panel">
        <div class="panel-heading">
            <h1>{{"article.plural"|trans}} / {{"status.inreview"|trans}} <small>These articlese are in review process.</small></h1>
        </div>

        <div class="panel-body">
            {% for entity in entities %}
                <div class="list-group-item">
                    <h4>{{entity.title}} <small>{{entity.journal.title}}</small></h4>
                    <span>
                        <!-- /* TODO : author list */  -->
                    </span>

                {% endfor %}
                <hr>

            </div>
        </div>
    </div>
{% endblock %}
