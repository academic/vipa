{% extends '::ojsbase.html.twig' %}

{% set hideleft = 1 %}


{% block body -%}
    <h1>{{ content.title }} <a class="label label-default"
                               href="{{ url('ojs_journal_index',{journal_id:content.journal.id}) }}">( {{ content.journal.title }} )</a></h1>
    <div class="content">
        {{ content.content }}
        <br/>
        <br/>

        <div class="tags">
            <span class="glyphicon glyphicon-tags"></span>
            {{ content.tags }}
        </div>
    </div>
{% endblock %}
