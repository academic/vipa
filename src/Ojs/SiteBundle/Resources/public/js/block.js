// Generated by CoffeeScript 1.7.1
$(document).ready(function() {
  $(".blocklink_order_updater").on('change', function() {
    var field, id, value;
    value = $(this).val();
    id = $(this).data('link-id');
    field = $(this);
    $.ajax({
      url: "/block_link/order/" + id + "/" + value,
      type: 'POST',
      dataType: 'json',
      success: function(rd) {
        if (rd.status) {
          document.location.reload();
          return false;
        }
      }
    });
    return false;
  });
  return $(".block_order_updater").on('change', function() {
    var field, id, value;
    value = $(this).val();
    id = $(this).data('block-id');
    field = $(this);
    $.ajax({
      url: "/block/order/" + id + "/" + value,
      type: 'POST',
      dataType: 'json',
      success: function(rd) {
        if (rd.status) {
          document.location.reload();
          return false;
        }
      }
    });
    return false;
  });
});
