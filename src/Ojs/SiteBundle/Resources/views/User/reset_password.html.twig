{% extends "::ojsbase.html.twig" %}

{% block body %}
    <div class="row">
        <div class="container" style="margin-top:60px">
            <div class="col-md-4 col-md-offset-4">
                <div class="panel panel-info">
                    <div class="panel-heading">
                        <h1 class="panel-title"><i class="fa fa-key"></i> {{ "user.reset_password"|trans }}</h1>
                    </div>
                    <div class="panel-body">
                        <form action="{{ path('ojs_user_reset_password',{token: token}) }}" method="post">
                            {% include '::flashbag.html.twig' %}
                            <div class="form-group">
                                <label for="password">{{ "user.new_password"|trans }}</label>
                                <input tabindex="1" name="password" value="" type="password"
                                       class="form-control" style="border-radius:0" id="password">
                            </div>
                            <div class="form-group">
                                <label for="password_confirm">{{ "user.new_password_confirm"|trans }}</label>
                                <input tabindex="2" name="password_confirm" value="" type="password"
                                       class="form-control" style="border-radius:0" id="password_confirm">
                            </div>

                            <button type="submit" class="btn btn-sm btn-primary pull-right">
                                <i class="fa fa-sign-in"></i> {{ "user.reset_password"|trans }}
                            </button>
                            <a href="{{ path('register') }}"
                               class="btn btn-sm btn-default pull-left">{{ "user.new"|trans }}</a>
                            <a class="btn btn-sm btn-warning pull-left"
                               href="{{ orcidLoginUrl() }}">{{ "orcid.login"|trans }}</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>

    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
