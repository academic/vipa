{% extends '::ojsbase.html.twig' %}

{% block breadcrumb %}
    {% set list = [
    {'link': path('dashboard'), 'title': 'dashboard'|trans},
    {'link': path('admin_proxy'),'title': 'proxy'|trans},
    {'title': 'create'|trans}
    ] %}
    {{ breadcrumb(list) }}
{% endblock %}

{% block body -%}
    <h1>{{ "proxy.creation"|trans }}</h1>

    {{ form(form) }}

    {% set back_path = 'admin_proxy' %}
    {% include '::actionbuttons.html.twig' %}
{% endblock %}
