{% extends '::ojsbase.html.twig' %}

{% block body -%}
<div class="large-12 columns">
    <div class="panel">
        {% include '::flashbag.html.twig' %}
        <h1>{{ "role.edit"|trans }} #{{ entity.id }}</h1>

    {{ form(edit_form) }}

        <div class="record_actions">

                <a class="button" href="{{ path('role') }}">
                    {{"b"|trans}}
                </a>
                <a href="{{ path('role_delete', { 'id': entity.id }) }}">
                    Delete
                </a>

        </div>
    </div>
</div>
{% endblock %}
